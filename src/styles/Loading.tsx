import { StyleService } from '@ui-kitten/components';

export const LoadingStyles = StyleService.create({
    loadingContainer: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
        backgroundColor: 'background-basic-color-1',
    }
});